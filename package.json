{
  "name": "pomodoro-todo-app",
  "version": "1.0.0",
  "description": "A Pomodoro timer app with task management and history tracking",
  "main": "main.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "npm run build && electron .",
    "start-safe": "npm run build && set ELECTRON_DISABLE_GPU=1 && electron .",
    "dev": "npm run build && electron . --dev",
    "dev-safe": "npm run build && set ELECTRON_DISABLE_GPU=1 && electron . --dev",
    "dev:watch": "tsc --watch & electron . --dev",
    "dist": "npm run build && electron-builder --publish=never",
    "dist-portable": "npm run build && set CSC_IDENTITY_AUTO_DISCOVERY=false && electron-builder --win portable --publish=never"
  },
  "keywords": [
    "pomodoro",
    "todo",
    "task-management",
    "electron",
    "productivity"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.5.2",
    "electron": "^27.0.0",
    "electron-builder": "^24.6.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "build": {
    "appId": "com.yourname.pomodoro-todo-app",
    "productName": "Pomodoro Todo App",
    "directories": {
      "output": "dist"
    },
    "files": [
      "**/*",
      "!node_modules",
      "!dist"
    ],
    "forceCodeSigning": false,
    "win": {
      "target": "dir",
      "icon": "assets/icon.ico",
      "sign": false
    },
    "mac": {
      "target": "dmg",
      "icon": "assets/icon.icns"
    },
    "linux": {
      "target": "AppImage",
      "icon": "assets/icon.png"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    },
    "portable": {
      "artifactName": "${productName}-${version}-portable.exe"
    }
  },
  "dependencies": {
    "sql.js": "^1.13.0"
  }
}
